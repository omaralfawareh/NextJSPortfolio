This is a [Next.js](https://nextjs.org/) project using the pages router.

## Getting Started

First, run the development server:

```bash
npm install

npm run dev
```
All Page are statically generated at build time.

